---
"uico": minor
---

feat: adds base styles for input groups and legends within a `<fieldset>` element to style checkboxes and radios without extra styles.

- Adds padding either before or after the label to space it apart from the input while still being selectable between the elements
